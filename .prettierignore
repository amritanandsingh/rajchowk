node_modules
.next
.amplify
# Local editor/agent tooling state, not project source. Untracked, and its
# formatting is owned by the tool that writes it.
.claude
out
build
coverage
playwright-report
test-results
package-lock.json
amplify_outputs.json
# Generated by `npm run ci:outputs:sync`, same shape as amplify_outputs.json and
# ignored for the same reason: it is a 200 KB machine-written artifact whose
# formatting is owned by the generator, not by prettier.
tests/fixtures/ci-amplify-outputs.json
next-env.d.ts
*.tsbuildinfo
