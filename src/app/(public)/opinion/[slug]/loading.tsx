/**
 * Opinion pieces render through the news article page (this route re-exports
 * its default), so they need the same article-shaped skeleton rather than the
 * app-root six-card grid.
 */
export { default } from '../../news/[slug]/loading'
